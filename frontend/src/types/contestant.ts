export type Contestant = {
    votes: number;
    id: string,
    name: string,
    video_url: string,
    thumbnail_url: string,
    verified_votes: number,
    has_ended: boolean,
    updated_at: Date
}
